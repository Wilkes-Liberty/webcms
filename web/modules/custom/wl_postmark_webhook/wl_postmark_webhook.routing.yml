wl_postmark_webhook.receive:
  path: '/api/webhooks/postmark/{secret}'
  defaults:
    _controller: '\Drupal\wl_postmark_webhook\Controller\PostmarkWebhookController::receive'
  methods: [POST]
  requirements:
    _access: 'TRUE'
  options:
    no_cache: TRUE
